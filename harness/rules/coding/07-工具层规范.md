# 编码规范 - 工具层规范

## 工具函数设计

- **纯函数优先**: 优先使用纯函数，避免副作用
- **无状态**: 工具函数不应依赖全局状态
- **单一职责**: 每个函数只做一件事

```typescript
export function formatFileSize(bytes: number): string {
  if (bytes === 0) return '0 B';
  
  const k = 1024;
  const sizes = ['B', 'KB', 'MB', 'GB'];
  const i = Math.floor(Math.log(bytes) / Math.log(k));
  
  return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
}
```

## 配置管理

- **集中管理**: 所有配置在`src/lib/config.ts`中管理
- **类型定义**: 使用TypeScript定义配置类型
- **默认值**: 为配置项提供合理的默认值

```typescript
interface Config {
  redis: {
    host: string;
    port: number;
    password?: string;
  };
  ai: {
    imageApiUrl: string;
    imageApiKey: string;
    promptApiUrl: string;
    promptApiKey: string;
  };
  app: {
    url: string;
    uploadDir: string;
    maxFileSize: number;
  };
}

export const config: Config = {
  redis: {
    host: process.env.REDIS_HOST || 'localhost',
    port: parseInt(process.env.REDIS_PORT || '6379'),
    password: process.env.REDIS_PASSWORD,
  },
  ai: {
    imageApiUrl: process.env.AI_IMAGE_API_URL || '',
    imageApiKey: process.env.AI_IMAGE_API_KEY || '',
    promptApiUrl: process.env.AI_PROMPT_API_URL || '',
    promptApiKey: process.env.AI_PROMPT_API_KEY || '',
  },
  app: {
    url: process.env.NEXT_PUBLIC_APP_URL || 'http://localhost:3000',
    uploadDir: process.env.UPLOAD_DIR || './public/uploads',
    maxFileSize: parseInt(process.env.MAX_FILE_SIZE || '10485760'),
  },
};
```

---

**相关规范**: [队列层规范](06-队列层规范.md) | [测试规范](08-测试规范.md)